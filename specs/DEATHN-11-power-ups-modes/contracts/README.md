# Contracts

No external interface contracts for this feature. The game is a self-contained local application with no public API, CLI commands, or network endpoints. The only persistence interface is the high score file format, which is documented in `data-model.md` §8.
