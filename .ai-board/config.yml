version: 1
project:
  name: "death-note"
  language: zig
  framework: none
runtime:
  manager: zig
commands:
  install: "zig build"
  test: "zig build test"
  type_check: "zig build --summary all"
  lint: "zig fmt --check ."
testing:
  e2e: false
  framework: zig-test
agent:
  cli: claude-code
